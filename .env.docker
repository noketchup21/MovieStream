# MongoDB
MONGO_USERNAME=a
MONGO_PASSWORD=a
MONGO_DATABASE=a

# JWT Secrets (change these!)
SECRET_KEY=a
SECRET_REFRESH_KEY=a

# Email (Gmail)
SMTP_EMAIL=a
SMTP_PASSWORD=a
SMTP_HOST=smtp.gmail.com

# Gemini AI
GEMINI_API_KEY=a
BASE_PROMPT_TEMPLATE=a

# App Config
APP_BASE_URL=a
ALLOWED_ORIGINS=a
RECOMMENDED_MOVIE_LIMIT=5

# Frontend
VITE_API_BASE_URL=a